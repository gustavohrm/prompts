interface:
  display_name: "Writing Specs"
  short_description: "Write approved designs as implementation-ready specs"
  default_prompt: "Use $writing-specs to write the approved design to a spec with a status, review it for gaps, and only proceed once the spec is documented as approved unless the user explicitly says otherwise."
